// Template for SupabaseConfig.swift (which is gitignored).
// Copy this file to SupabaseConfig.swift and fill in your project's values.
// The anon key is the PUBLIC, RLS-gated client key — never the service key.
//
// import Foundation
//
// enum SupabaseConfig {
//     static let url = URL(string: "https://YOUR-PROJECT.supabase.co")!
//     static let anonKey = "YOUR_PUBLIC_ANON_KEY"
// }
